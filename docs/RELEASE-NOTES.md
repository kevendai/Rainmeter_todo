# Release Notes

## 2.0.3 - 2026-09-16

- 修复插件市场与“从本地安装”安装 `.rwplugin` 时报“系统找不到指定的文件。”的问题。根因是插件安装由一段 PowerShell 脚本执行，而主程序硬编码调用 `pwsh.exe`（PowerShell 7）：只装了 Windows PowerShell 5.1 的机器上并不存在这个程序，`Process.Start` 会直接以 WinError 2 失败，插件始终装不上。现在包的校验、安全解压与落盘全部改由主程序自身用 C# 完成，不再依赖任何外部 PowerShell，也不再受脚本文件编码影响。
- 同一条链路的另外两个隐患一并修掉：安装脚本此前是 UTF-8 无 BOM，Windows PowerShell 5.1 会按系统代码页读取，中文提示会把语句拆坏，即使回退到 5.1 也仍然失败；把暂存目录搬到插件目录时用的是 `Directory.Move`，当 `%TEMP%` 与插件目录不在同一个卷时必然失败，现已改为跨卷复制后删除。
- 打包与解压收敛为一份共享实现：更新器与插件安装器共用同一个手写 ZIP 读取器，路径越界、符号链接、ZIP64、重复条目、CRC 校验统一在这一层拦截，不再有两份实现各自演化。
- 皮肤目录定位完全以 Rainmeter 自己的配置为准：优先读便携安装中与 `Rainmeter.exe` 同目录的 `Rainmeter.ini`，其次使用该目录下的 `Skins`，之后才回退到 `%APPDATA%\Rainmeter\Rainmeter.ini` 的 `SkinPath`。标准版（皮肤在 `Documents\Rainmeter\Skins`）与便携版（皮肤在程序目录下的 `Skins`）都能正确定位，不会把标准版的路径套用到便携版，也不写死任何安装位置；`SkinPath` 为相对路径时按 ini 所在目录解析。
- 插件安装的回归测试从 PowerShell 换成 C# 探针（`tests/PluginInstallerProbe.cs`，19 项断言）：自带极简 ZIP 写入器，可直接构造路径穿越、缺少清单、字段非法等坏包，并覆盖 SHA256 校验、重装幂等与 `enabled` 开关保留；随包的 PowerShell 安装器与旧测试已删除。
- 修复部分机器上所有插件“一次都跑不起来”的问题：启动插件进程时会把 `RW_PLUGIN_DATA_DIR`、`RW_WINDOW_SCALE` 写进子进程环境，而 `ProcessStartInfo.EnvironmentVariables` 内部用 `StringDictionary` 保存键（键统一转小写），一旦宿主的环境块里同时存在大小写不同的同名变量（例如安装器、Git Bash 或 CI 代理额外注入了一份 `PATH`，于是 `Path` 与 `PATH` 并存），写入会直接抛 `ArgumentException: 已添加项。字典中的关键字:"PATH"所添加的关键字:"Path"`，插件安装、转换、取值全部失败。现在由主程序自己构造一份大小写去重后的干净环境块（同名变量保留系统最先解析到的那一项，其余变量原样保留）再交给子进程，`tests/PluginEnvironmentProbe.cs` 覆盖去重构造与“环境确实送达子进程”。

## 2.0.2 - 2026-09-16

- 修复插件市场打开时报“市场不可用：传入的对象无效”的问题：索引改为下载原始字节并按 UTF-8 显式解码，不再依赖系统默认代码页，避免中文条目被写成乱码后落盘。只有解析成功的索引才会写入 `registry-cache.json`；缓存本身损坏时自动删除并回退内置 `plugin-registry-v1.json`，市场不会再被一份坏缓存永久卡住。
- 修复更新器替换皮肤目录时“对路径 `Skins\Todo` 的访问被拒绝”的问题。根因是更新链的工作目录：Rainmeter 启动皮肤宿主时给的工作目录位于皮肤目录内部，而启动子进程时不指定工作目录会让整条链（宿主 → 更新器 → 包内新更新器）继承它；Windows 不允许重命名“某个进程的当前目录”所在的目录，所以无论 Rainmeter 多早退出，目录交换都会失败。现在更新器一启动就把自己的工作目录切到临时目录，委派子进程时也显式指定中性工作目录，皮肤宿主与兼容脚本同样处理，并让 Rainmeter 也从中性目录启动，避免下次更新再次继承。
- 同一批修复：目录解锁检测改为真正的重命名探测（原先在目录内创建探针文件，即使目录被锁也会“通过”，导致误判并拖到 30 秒重试后才失败），失败时提示明确原因；安装失败会自动重新启动 Rainmeter，不再把桌面留在没有任何磁贴的状态；更新器自身被中断在探测窗口时也能恢复皮肤目录。
- v1.3.5 继续沿用单跳兼容通道：`full` / `lite` 兼容包不含任何程序本体，只有空壳 `Install-Skins.ps1` 和兼容更新器；更新时直接下载并校验 2.0.2 正式包后安装。
- 汇总此前未随任何版本发布的更新器加固：自更新与安装子进程检查退出码，失败时弹窗并写入 `Updater/last-error.log`；正式包改由随包的 `UpdaterHost.exe` 负责下载、TLS、SHA256 校验、安全解压与事务安装。
## 2.0.1 - 2026-09-14

- “SSDP 服务器 IP”改为插件市场中的私人插件，简介明确标注不随主程序捆绑；新安装包只捆绑 arXiv 与 Calendar-to-Todo。
- 插件市场列表显示注册表 description，方便在安装前识别私人插件及其用途。
- v1.3.5 的 full/lite 入口改为单跳兼容包：包内不含任何本体，只带空壳 `Install-Skins.ps1` 和兼容更新器，由它下载并校验 2.0.1 正式包后直接安装。

## 2.0.0 - 2026-09-14

- 新增按需启动的 `PluginHost.exe`、Plugin API v1、JSON Lines 进程协议、UTF-8 管道、超时/取消、任务状态、错误日志和插件级同步互斥。
- Todo 数据升级为 v3：外部身份统一使用 `origin.plugin_id + external_id`，行为使用 `policy`，核心不再识别 arXiv/CalDAV 业务名；迁移前创建时间戳备份并写入幂等完成标记。
- arXiv、Calendar-to-Todo 成为主程序捆绑的独立进程插件；“SSDP 服务器 IP”作为市场中的私人插件按需安装，不随主程序捆绑。TodoHost 不再编译论文抓取/RSS 服务，CalendarHost 不再直接读写 `tasks.json`。
- 新增插件管理 UI：安装、启用、禁用、Schema 配置、actions、取消、更新、卸载及二次删除数据确认；市场仅显示 `official=true` 条目并强制 GitHub HTTPS 与 SHA256。
- 新增 `.rwplugin` 安全 staging 安装器、官方插件包/哈希/锁文件构建、GitHub Pages 注册表模板和公开 Schema/API 文档。
- 新增 `PluginValues.json` / `PluginValues.inc` 桥接、TTL 调度以及失败时保留最后成功值的 Stale 标记。
- `.rwbackup` 配置升级为 2.0，包含插件启用状态、配置和重新加密的 secret，并继续导入 1.0 备份。
- 桌面磁贴与管理/编辑窗口新增两个独立缩放选项；窗口比例保存在 `ui-window-scale.txt`，旧安装默认保持原有窗口大小，并纳入部署保留、自动更新和 rwbackup。
- 为 v1.3.5 保留显式两跳升级：v2.0.0 标签下的旧式 full/lite 入口先安装 v1.4.4；用户再次检查更新后，再由统一更新器校验 SHA256 并升级到 v2.0.0。

## 1.5.4 - 2026-09-11

- 修复待办管理、论文 Worker、自动归档和备份导入之间的并发整文件写入竞态：所有状态变更统一在全局锁内重新加载、修改并原子提交，避免过期快照覆盖新数据。
- Todo、Calendar 本地部署和自动更新改用暂存目录、目录快照与失败回滚；替换前后校验用户数据哈希，意外中断后可恢复未完成事务。
- CalDAV 日程服务器继续允许直接使用 HTTP，不显示安全提醒；DeepSeek 和文件服务器保持原有连接行为。
- 更新包改从 GitHub Release 资产下载并强制验证配套 SHA256；历史二进制包移出 Git 仓库，仅保留约 6 KB 的 v1.5.4 一次性迁移引导包供旧升级器过渡，构建流程自动生成所有校验文件。
- DeepSeek、CalDAV、文件服务器与更新检查移到后台线程，移除 `Application.DoEvents()` 重入；配置解密失败会在设置状态区显示原因。
- 论文 RSS 按配置文件修改时间缓存解密结果；论文自测改在隔离临时目录运行，不再改写生产凭据。
- 新增显式源文件清单的 .NET Framework 项目与解决方案，构建、部署和自测统一走项目文件；拆分核心 DAV 请求逻辑并收敛设置页网络操作。
- Rainmeter 路径统一通过运行进程、便携目录和系统 Program Files 发现，不再包含个人机器的硬编码安装路径；周报脚本移入 `scripts`。

## 1.5.3 - 2026-09-11

- 将桌面磁贴缩放与设置、编辑、管理窗口缩放分离：手动比例只调整 Todo 和 Calendar 磁贴，窗口保持 2K 设计尺寸并继续适配 Windows 显示缩放；自动模式仍根据屏幕分辨率统一计算。
- Todo 设置页明确显示当前磁贴比例并更新说明文字，应用缩放后同时重新渲染 Todo 与 Calendar 磁贴。
- Calendar 本地部署脚本现在优先读取 Rainmeter 配置中的 `SkinPath`，避免自定义皮肤目录时仍部署到程序安装目录。

## 1.5.2 - 2026-08-12

- 修复本地论文 RSS 在 20:00 后因同步窗口关闭而错误返回空 feed：`/paper/rss` 现在全天读取当天 PaperCache 与未完成论文待办，`healthz.in_window` 仅汇报后台自动同步窗口状态，不再控制订阅输出。
- 新建和编辑日程新增周期设置，支持按日、周、月、年重复；每周可选择多个星期，每月可选择日期，每年可选择月份和日期，结束方式可选永不结束或直接输入重复次数。本地日历会在本机展开各期，CalDAV 日历会写入标准 `RRULE` 并同步到远端。
- 周期日程支持编辑和删除整个系列，也可仅在本机隐藏单次发生；外部日历中的复杂周期规则、排除日期和例外实例会保持原样，避免简化保存时破坏远端系列。
- 完善周期日程的保存安全性：CalDAV 周期创建失败时不再静默降级为本地单次日程；编辑或删除前会复核远端资源，避免同步元数据缺失时误覆盖或误删整个系列。

## 1.5.1 - 2026-08-05

- 论文 RSS 改为标准 RSS 2.0 字段与 `paper` 命名空间扩展并存：保留通用阅读器可识别的标题、链接、HTML 摘要、发布时间和分类，同时提供英文原标题、顺序作者、arXiv ID、标题分、摘要分、完整摘要、摘要页和 PDF 直链等结构化字段。
- `description` 使用 CDATA 包装的 HTML，并把英文摘要正文放在最前面，确保只截取前几百字符的订阅端仍能直接取得摘要；元数据不再混入展示字段。
- RSS 根节点显式声明 `paper` 与 `dc` 前缀且不设置默认命名空间，保持传统 RSS XPath 和阅读器兼容性；CDATA 中出现 `]]>` 时也会安全拆分，继续输出合法 XML。

## 1.5.0 - 2026-08-05

- 修复删除周期自动转入规则后，因为当前一期仍保留转换记录，日程详情页隐藏“转为待办”按钮而无法重新建立规则的问题；此时详情页改为显示“恢复自动转入”，只恢复未来周期规则而不重复创建当前待办。
- Todo 设置“关于”页新增用户配置导出与导入：默认备份 CalDAV、论文、文件服务器、标题翻译、界面缩放和自动转入规则，可选同时备份待办与本地日程。`.rwbackup` 使用独立密码加密，导入时在新电脑上重新生成 DPAPI CurrentUser 密文；配置结构版本从 `1.0` 开始独立记录，为后续字段迁移保留兼容入口。
- 论文推荐新增默认关闭的本地 RSS 服务选项。启用后由 `TodoHost.exe` 的单实例后台模式固定监听 `127.0.0.1:8891`，提供 `/paper/rss`、`/healthz`、`min_score` 和 `limit`；端口占用或启动失败会在设置窗口明确提示并自动保持关闭。
- RSS 仅在本地时间 08:00（含）至 20:00（不含）发布今日未完成推荐；PaperCache 提供当天排序、摘要和分数，`tasks.json` 提供中文标题及完成状态。当天缓存缺失时只尝试现有文件服务器，成功后同步导入待办并刷新磁贴，失败或远端不存在时返回合法空 feed，不会自动调用 DeepSeek。
- RSS XML 由标准 XML 写入器生成并使用 RFC 822 时间格式，支持标题与摘要特殊字符转义；channel 不输出冗余 description，item description 使用英文标签输出缓存中的 title、authors、arXiv ID、categories、两阶段分数、URL、PDF 和 abstract，arXiv 分类同时写入标准 category 元素；已完成或自动归档论文不会继续出现在 feed 中。

## 1.4.5 - 2026-07-17

- 修复应用界面缩放后窗口立即使用新比例、但 Todo/Calendar 磁贴可能仍停留在旧生成文件的问题：两个磁贴生成完成后改为统一执行一次 Rainmeter 全局刷新，并新增 70%–125% 的真实磁贴宽度渲染检查，与现有窗口布局探针一起验证。
- 修复 2880×1800、Windows 200% 等高 DPI 笔记本上磁贴比例合适但管理/设置窗口明显偏小的问题：窗口在项目界面比例之外，会相对 125% 设计基准补偿显示器 DPI；例如 75% 界面比例在 200% Windows 缩放下使用 120% 的窗口布局比例，磁贴仍保持 75%。
- 修复未配置 CalDAV 或其他电脑上的 Rainmeter 安装路径不同，导致磁贴永久停在“正在同步日程/正在刷新”的问题：刷新按钮不再提前覆盖持久状态文字；后端优先定位正在运行的 Rainmeter 或皮肤旁的便携版程序，并验证无凭据同步会落回“CalDAV 未连接”。

## 1.4.4 - 2026-07-16

- 修复 Windows 显示缩放为 150%/200% 时 WinForms 字体继续按 DPI 放大、但窗口和控件仍保持固定像素尺寸而造成标题、月份、筛选按钮和底栏文字被裁切的问题；窗口字体现在以 2560×1440、125% 显示缩放的设计基准转换为像素字体，由项目界面比例统一控制，并新增 192 DPI 离屏文字裁切检查。
- 初次安装统一改为直接双击 `.rmskin`；用户可下载的完整 ZIP 不再提供 `Install-Skins.ps1` 安装入口。自动更新仍使用 ZIP 作为传输包，并改为通过 `manifest.json`、`Skins` 与包内更新器识别完整更新包。
- `.rmskin`、自动更新包和本地 `Deploy-Todo.ps1` 现在都会明确写入 `Todo\@Resources\Updater\RainmeterDesktopWidgetsUpdater.ps1`；构建阶段会校验该文件已进入 `.rmskin`，避免直接部署遗漏独立升级器。

## 1.4.3 - 2026-07-16

- Todo 与 Calendar 新增统一界面缩放：以 2560×1440 为 100% 基准，自动模式会在 1920×1080 上使用约 75%，同时缩放 Rainmeter 磁贴、文字、图标、行高以及 WinForms 编辑/设置/管理窗口。
- Todo 设置的“关于”页新增自动、75%、80%、90%、100%、110% 和 125% 缩放选项；比例保存在独立的 `ui-scale.txt` 中，本地部署和自动更新都会保留用户选择。
- 后端窗口启动时启用 DPI awareness，把清晰度缩放与分辨率布局缩放分开，避免依赖 Windows 位图放大造成文字发糊。
- 修复窗口首次缩放后动态重建的控件仍保持 100% 的问题：日程管理翻月、切换“今天/未来7天/全部时间”、搜索和刷新后，日期格与日程行会继续使用当前比例；待办管理筛选或重载后的任务行也保持一致。
- 修复缩小比例下圆角 Region、自绘日历/账号图标、时间滑杆和原生复选框仍使用原始尺寸造成的裁切；“全部时间”、标签“展开/收起”和设置页下拉框也增加了缩放后的安全空间。
- 修复新增待办标签展开后再收起时首行标签全部隐藏、标签面板恢复为未缩放高度并覆盖“备注”的问题；展开高度现在按实际标签行数计算。
- 新增 70%、75%、80%、90%、100%、110%、125% 的交互式布局探针，覆盖标签展开/收起、待办列表重载、日历日期重建、全部时间切换和日程设置图标绘制。

## 1.4.2 - 2026-07-16

- 修复发布包安装入口在未显式传入 `RainmeterRoot` 时仍向子 PowerShell 传递空参数、导致 `Install-Skins.ps1 -Activate` 直接报缺少参数的问题；现在会正常进入皮肤目录选择。
- 修复 `.rmskin` 仅由普通 ZIP 改后缀、缺少 Rainmeter Skin Packager 校验 footer 而被 SkinInstaller 判定为无效包的问题。
- 新安装或没有旧论文配置时，“包含分类”和“排除分类”默认均为空；包含分类留空会抓取全部计算机科学分类，排除分类留空则不排除任何分类。已有用户保存的分类配置不会被更新或部署覆盖。

## 1.4.1 - 2026-07-16

- DeepSeek 论文评分请求启用 thinking 模式，提高复杂评分规则的遵循稳定性；用户自行保存的提示词与凭据继续由 DPAPI 配置保留，不写入发布包。
- 刷新顺序改为完整本地论文缓存优先：当天已有完整 JSON 时不再下载远端同名文件，只在远端缺失时补传本地文件，避免后续连接文件服务器后把本地评分结果替换或混入远端推荐。
- 本地评分完成后，上传前先检查文件服务器是否已有同名文件：不存在时直接同步，存在时弹窗询问是否覆盖；选择不覆盖会保留远端原文件并继续导入本地待办。
- “论文推荐”页面增加“重新爬取并打分”：自动保存当前论文设置、清除当天缓存并重新调用 DeepSeek；旧推荐保留到新评分完整成功后再原子替换，失败时不丢失现有待办。
- 提示词按钮改为“在光标处插入论文”；鼠标位于提示词框时优先滚动框内内容，仅在到顶继续上滚或到底继续下滚时滚动外层设置页面。
- 论文任务状态区分设置页短文案和磁贴详细文案；磁贴下载状态恢复“已下载 MB / 总 MB”，并以轻量 meter 更新代替反复刷新整张皮肤。
- arXiv RSS 优先使用官方 `export.arxiv.org` 入口，并在 DNS、连接或临时 HTTP 错误时自动切换 `rss.arxiv.org` 后重试；最终失败时显示明确的网络/DNS 提示。
- DeepSeek 评分确认窗口使用来自 iconfont.cn 的本地 AI 自动评分图标，移除标题和底部冗余说明，扩大正文高度避免第二行裁切，并修正主按钮文字的垂直对齐。
- arXiv 抓取改为按“包含分类”请求官方多分类 RSS，不再下载整个 `cs` 大类；修复新版 RSS 使用 `pubDate` 导致全部论文被日期过滤、无法进入 DeepSeek 评分的问题。
- arXiv XML 改为边下载边解析，不再同时保留完整字节数组和字符串副本；下载状态精简为“正在获取 MM-dd 的论文”。
- 论文 Worker 的全局互斥锁前置到刷新决策：抓取或评分期间再次点击刷新只刷新界面，不再访问远端或启动第二份任务。
- DeepSeek 费用确认窗口增加“今日不再提醒”；选择后当天不再提示或本地评分，但每次刷新仍优先检查远端服务器。
- 论文设置页的当前任务状态增加实时进度条；arXiv 下载按接收字节显示进度，标题和摘要评分按已完成论文数显示进度。
- 论文设置保存成功后只显示“设置已保存”，并移除“清除论文配置”按钮。
- 标题和摘要评分提供开箱即用的通用非交互式 JSON 默认文本；设置页可通过“在光标处插入论文”按钮把 `<INSERT_PAPER_LIST_HERE>` 放到当前光标位置，评分时用当前批次论文替换该占位符。
- 鼠标位于多行提示词输入框上时，滚轮会继续滚动外层“筛选与评分”页面，避免大页面滚动条失去滚轮响应。
- 发布包只编译一份统一版；旧 full/lite 升级器下载的同名 zip 改为轻量升级器引导包，引导新版升级器继续下载唯一的统一版 zip，不再复制完整皮肤包或生成 full/lite `.rmskin`。

## 1.4.0 - 2026-07-16

- Todo 新增完全内置的 C# arXiv 抓取与 DeepSeek 两阶段并发评分，不再依赖外部 Python 工具；支持严格批次 ID/分数校验、429/5xx 退避重试、失败批次拆分和断点续跑。
- 论文刷新改为“远端文件 → 本地完整缓存 → 用户确认后后台评分”，启动时只在 08:00–20:00 读取已有数据，不会自动产生评分费用；后台进度会显示在磁贴状态栏。
- `paper-sync.secret` 升级为版本化 DPAPI 配置，统一保存论文总开关、DeepSeek、文件服务器、分类、提示词、阈值、批大小、并发、导入数量和缓存天数，并兼容迁移旧 File Browser 配置。
- Todo 设置窗口改为六页纵向导航，新增论文推荐、DeepSeek API、筛选评分和文件同步页面，保留标题翻译和关于/更新页面。
- full/lite 编译差异已合并为一个运行时可配置的统一版本；正式生成统一 zip / `.rmskin`，同时为旧升级器保留内容完全相同的 full/lite 文件名别名。
- Calendar 自定义“开始前提醒”统一保存在提醒分钟列表中，编辑已有自定义提醒时会稳定显示并可直接删除，避免额外的启用/禁用状态造成保存结果不一致。

## 1.3.6 - 2026-07-10

- 修复“全部任务”管理器在列表下方删除、修改或批量操作后刷新时未先复位旧滚动偏移的问题；现在会保留当前浏览位置，并避免表头上方出现大块空白。

## 1.3.5 - 2026-07-08

- 新建 CalDAV 日程时若远端连接超时或保存失败，会先把当前输入保存为本地日程并提示原因，避免保存窗口关闭后内容丢失；本地日程编辑时也可切换到 CalDAV 日历完成后续同步。
- 日历部署脚本不再从仓库工作区回填 `caldav.secret` 到 live 目录，避免旧的本地密钥或旧服务器地址在重新部署时被意外复活。
- 本地 Todo 部署脚本现在会像自动更新器一样保留 live `caldav.secret`、`translation.secret` 和 `paper-sync.secret`，避免源码工作区残留的旧 DPAPI 密钥在开发部署时覆盖当前配置。

## 1.3.4 - 2026-06-29

- 统一版本来源：根目录 `VERSION` 现在驱动 Rainmeter 皮肤管理器元数据、后端 About/更新检查版本、发布包 `manifest.json` 和包文件名，减少发版时版本号漂移。
- 发布包新增 `full` / `lite` 两个 `.rmskin` 初始安装包，已安装 Rainmeter 的用户可以直接用 `SkinInstaller.exe` 安装；自动更新仍使用原有 zip + updater 路径以保留旧数据。
- 修复本机部署脚本漏编译拆分后的 `Todo*.cs` / `Calendar*.cs` 文件风险，并在部署时同步写入 `app-version.txt` 和皮肤 `Version=`。

## 1.3.3 - 2026-06-29

- 优化设置页“检查更新”流程：设置页已经确认更新后，独立升级器会直接下载并部署，不再额外弹出第二个确认框。
- 升级器下载更新包时改用兼容的 BasicParsing 网络请求路径，避免 Windows PowerShell 走旧网页解析组件时出现额外安全/脚本提示。
- 保持旧升级器兼容：`Install-Skins.ps1` 根入口、包内 `Updater/RainmeterDesktopWidgetsUpdater.ps1`、raw tag 包路径和 GitHub Release 资产布局不变。

## 1.3.2 - 2026-06-29

- 新增轻量服务层拆分：`TodoService.cs` 承接待办交互式命令和锁定状态执行器，`CalendarService.cs` 承接日历同步、CalDAV 保存/删除和日程转待办相关的编排逻辑。
- 保持 `TodoApp.cs` / `CalendarApp.cs` 更接近命令入口，协议解析、ICS 读写、窗口绘制和 Rainmeter 渲染继续由各自模块负责，为后续脱离 Rainmeter 宿主预留边界。
- 继续保留 1.2.x/1.3.x 升级器兼容：发布包根目录仍提供 `Install-Skins.ps1`，包内仍包含 `Updater/RainmeterDesktopWidgetsUpdater.ps1`，raw tag 包路径和 `manifest.json` 结构不变。

## 1.3.1 - 2026-06-29

- 拆分 Calendar 后端结构：`CalendarApp.cs` 保留命令入口，日历状态存储、CalDAV 同步、日程转待办规则、ICS 读写、WinForms 管理窗口和 Rainmeter 渲染分别移动到独立的 `Calendar*.cs` 模块。
- 打包和后端测试脚本改为自动编译所有 `Calendar*.cs`，与 1.3.0 的 Todo 模块化方式一致，降低后续继续拆分时漏编译新文件的风险。
- 保持现有日程 UI、CalDAV 数据格式、Rainmeter 皮肤入口和旧升级器包路径兼容；本版本只调整代码组织，不迁移用户数据。

## 1.3.0 - 2026-06-29

- 拆分 Todo 后端结构：`TodoApp.cs` 现在保留命令入口，待办数据存储、业务规则、Rainmeter 渲染、设置/管理窗口和更新/论文同步逻辑分别移动到独立 `Todo*.cs` 模块，为后续从 Rainmeter 宿主中脱离做准备。
- 保持现有 WinForms 界面、Rainmeter 磁贴、数据文件和命令参数兼容；本版本不主动改版 UI，也不迁移用户数据格式。
- 打包和测试脚本改为编译所有 `Todo*.cs` 源文件，避免后续继续模块化时遗漏新文件。

## 1.2.4 - 2026-06-29

- 调整“检查更新”交互：按钮先在设置窗口内轻量检查 GitHub 最新 tag，只有检测到新版本时才弹出更新确认并启动独立升级器；已是最新版本时只更新设置页状态，不再额外打开 PowerShell 窗口或弹出“Already on latest version”对话框。

## 1.2.3 - 2026-06-29

- 修复 1.2.2 更新器在安装包入口和升级器自升级后部署本体时传递 `-Activate` 开关参数的方式，避免 Windows PowerShell 将 `System.String` 转换为 `SwitchParameter` 失败。

## 1.2.2 - 2026-06-29

- 新增独立升级器脚本 `Updater/RainmeterDesktopWidgetsUpdater.ps1`。`TodoHost.exe` 的“检查更新”按钮现在只负责启动升级器，版本检查、下载、解压、部署都由升级器脚本处理，后续升级策略可以不重编主程序。
- 发布包继续保留 `Install-Skins.ps1` 兼容入口，但安装入口会委托给包内升级器执行部署；从旧版本更新到 1.2.2 时会同时把独立升级器安装到 `Todo/@Resources/Updater/`。
- 升级器支持先从新包中更新已安装升级器，再由已安装升级器部署本体，为之后 UI 与后端分离后的升级流程预留兼容路径。

## 1.2.1 - 2026-06-29

- 新建日程不再默认启用“15 分钟前”提醒；只有用户手动选择常用提醒或添加自定义提醒时，才会写入本地日程和 CalDAV `VALARM`。
- CalDAV 日程保存成功后会立即刷新本地日程缓存；周期日程修改不再先清空同系列缓存，避免保存后短暂消失、必须手动同步才重新出现。
- 保存 CalDAV 凭据时会清空旧日历发现缓存；同步时也只复用属于当前保存服务器的 `calendar_url`，避免地址改成新服务器后又沿用旧服务器。

## 1.2.0 - 2026-06-28

- CalDAV 设置不再内置默认服务器地址；保存、测试连接和后台发现都要求用户显式填写 CalDAV 地址，避免误连到开发环境服务器。
- 新建/编辑日程的提醒区改为常用提醒胶囊 + 展开区：常用提醒直接勾选，自定义“开始前提醒”通过添加提醒弹窗创建，CalDAV 额外格式提醒只在展开区显示并允许删除。
- 删除 CalDAV 额外提醒后保存会二次确认，确认后会从远端 VALARM 中移除；这些结束前提醒或固定时间提醒仍会被解析和保留，但界面不支持重新创建或修改。
- 日程管理的“今天 / 未来7天 / 全部时间”分段按钮修复鼠标经过后背景停留为白色的问题，hover 后会恢复到正确的选中或灰色状态。
- 新建/编辑日程新增提醒设置，支持 5 分钟前、15 分钟前、30 分钟前、1 小时前、5 小时前、1 天前等多个开始前提醒，并写入 CalDAV `VALARM`。
- CalDAV 提醒解析支持多个 `VALARM`，包括 `TRIGGER;VALUE=DATE-TIME` 这类绝对时间提醒；非“日程开始前”的提醒在编辑界面仅允许删除，不允许新增或修改，保存时未删除会原样保留。
- 日程转待办继续使用日程开始时间和最早提醒时间中更早的一个作为待办开始时间；自动转待办扫描也会纳入已到提醒日期的未来日程，避免提前提醒的周期日程等到发生当天才生成待办。

## 1.1.7 - 2026-06-28

- 修复标准 Rainmeter 安装无法通过内置更新定位目录的问题：更新器现在按当前皮肤所在的 `Skins` 目录定位皮肤库，不再要求皮肤库上级目录必须包含 `Rainmeter.exe`。
- 安装脚本支持便携安装目录、`Documents\Rainmeter` 皮肤库目录或直接输入 `Skins` 目录；`Rainmeter.exe` 会从便携目录、正在运行的 Rainmeter 进程或注册表中单独查找，用于重启和刷新皮肤。

## 1.1.6 - 2026-06-28

- 修复日程管理面板点击“刷新同步”后按钮一直停留在后台同步状态的问题；同步完成后会自动重新读取缓存并恢复按钮。
- 修复缓存日历地址与设置页 CalDAV 地址不一致时，事件资源 href 可能回退到旧服务地址的问题，事件地址现在跟随实际查询的日历 URL 解析。
- 更新器解压安装包后会对本次解压出的文件执行解除阻止，安装脚本复制到 Rainmeter 目录后也会清理文件的 Zone.Identifier，避免其他电脑上打开 `TodoHost.exe` / `CalendarHost.exe` 时出现“未知发布者”安全警告。
- 自动更新完成后只清理本次创建的临时解压目录，以及文件名匹配 `rainmeter-desktop-widgets-*.zip` 的本次下载包，不会扫描或删除其它临时文件。
- 安装脚本复制新文件前会先关闭当前 Rainmeter 目录下的 `TodoHost.exe` 和 `CalendarHost.exe`，避免日程管理、编辑或后台同步窗口占用旧程序导致只更新版本号、日程端未替换的半更新状态。

## 1.1.5 - 2026-06-27

- 优化日程管理窗口启动路径：管理和设置界面打开时只短暂等待同步锁，后台正在同步时也会先显示本地缓存，避免 CalDAV 网络请求拖慢窗口出现。
- 日程管理里的“刷新同步”改为后台启动同步进程，不再在窗口 UI 线程里直接执行网络同步，点击后窗口保持可操作。
- CalDAV 同步优先复用已缓存的日历地址，减少每次同步前重复发现日历的开销；缓存地址失效时再回退到服务发现。
- CalDAV 未来 21 天拉取从逐日 21 次查询改为一次 21 天区间查询，保留周期日程展开能力，同时显著减少网络往返。
- 管理窗口关闭时只有发生新建、编辑或设置变更才触发重新渲染和刷新，减少无改动关闭后的拖尾卡顿。

## 1.1.4 - 2026-06-27

- 更新下载完成并启动自动部署后，不再弹出需要手动确认的信息框，避免确认框阻塞后续部署流程。
- 日程管理移除“按开始时间/按标题”排序下拉，固定按开始时间、结束时间、标题排序，并将“今天 / 未来7天 / 全部时间”改为分段胶囊按钮。
- 日程链接字段支持填写本地文件或文件夹路径；CalDAV 写入时本地路径保存到 `X-RAINMETER-LINK` 扩展字段，不再塞进标准 `URL` 字段导致同步端尝试解析本机路径。
- 缩短单日 CalDAV 查询超时时间，避免某一天查询卡住时拖慢整轮同步。
- 日程转待办时，待办开始时间改为日程开始时间和提醒时间中更早的一个。
- 日程转待办后会等待 Todo 渲染完成再刷新皮肤，新待办无需手动再刷新；Todo 右上角入口改为待办刷新，并在完整功能版中同时提醒论文同步系统检查今日论文。
- 日程详情窗口底部按钮改为右对齐紧凑按钮组；已转为待办时隐藏转换按钮，剩余按钮继续贴齐排列。

## 1.1.3 - 2026-06-27

- 内部亮色 UI 辅助类从 `DarkUi` 重命名为 `LightUi`，为后续深色模式预留 `DarkUi` 命名。
- 调整新建/编辑窗口时间滑条高度与字段间距，避免开始/结束时间遮挡地点；全天状态保留置灰时间滑条并修复按钮选中态可见性。
- 修正日程管理时间筛选：全部时间显示所有已同步日程，未来 7 天标题显示日期范围，跨日列表卡片显示具体日期与星期。
- 日程管理时间筛选顺序调整为“今天 / 未来7天 / 全部时间”，默认进入今天视图。
- 修复日程管理底部“设置”和“刷新同步”按钮鼠标经过后浅蓝背景丢失的问题。
- 调整日程管理右上角关闭按钮字体与内边距，使关闭图标居中显示。
- 修复日程设置“自动转入”页仍显示账号页“清除设置 / 测试连接 / 保存凭据”按钮的问题。
- 替换日程管理、待办/日程设置、全部任务和新增待办窗口左上角图标，并将对应 SVG 资源纳入项目。
- 新建日程窗口左上角改用独立的新建日程 SVG 图标。

## 1.1.2 - 2026-06-27

- CalDAV 同步范围扩展到未来 21 天并按天分片查询，发现阶段超时时会回退到已缓存的日历地址；部分日期超时时会保留已同步结果，自动转待办仍只处理当天发生的日程。
- 月历增加本地日历与 CalDAV 日历的多色日程点，今天未选中时使用浅蓝标记，选中日期继续使用蓝色实心圆。
- 增大并下移月历日程点，避免小点贴在日期圆底部导致看不清。
- 日历筛选精简为本地日历和 CalDAV 日历两个胶囊，两者同时勾选即为全部日程。
- 无地点日程改为更淡、更小的“无地点”提示，减少对标题层级的干扰。
- 调整日程管理副标题文案，并修正底部“设置 / 刷新同步 / 新建日程”按钮的居中与间距。
- 新建/编辑日程窗口改为圆角输入面板、胶囊全天按钮和 15 分钟时间菜单；标题设为必填，并将删除入口仅保留在编辑窗口底部。
- 新建日程默认选择 CalDAV 日历；日历选择改为本地/CalDAV 胶囊按钮，日期改为左右切换，时间改为 15 分钟粒度滑条，缓解表单拥挤和底部裁切。
- 时间滑条改为自绘圆角轨道与圆形拖拽点，日历胶囊去掉外框线并增强选中态文字可读性。
- 周期 CalDAV 日程支持确认后改写整组，保存时保留原始 RRULE 周期规则；编辑窗口底部删除按钮固定显示，不再被提示信息遮挡。
- 没有 DTEND 或 DTEND 等于 DTSTART 的日程默认按 1 小时显示。

## 1.1.1 - 2026-06-27

- 日程管理底部去掉“已选择/编辑”的批量任务逻辑，改为当前日期统计、设置、刷新同步和新建日程。
- 左侧月历改为自绘卡片风格，选中日期、高亮今天和有日程日期的小圆点与整体玻璃风格统一。
- 日历筛选改为胶囊卡片样式，只保留全部日程、本地日历和 CalDAV 日历。
- 修复日历筛选胶囊按钮鼠标经过后变白、文字几乎不可见的问题。
- 右侧顶部删除“日程列表/周视图/月视图”，保留当前日期标题、时间范围和排序。
- 日程卡片调整为横向时间段、标题、地点和日历标签；编辑仍保留在单条日程右侧。
- 调整日程管理图标为蓝色日历图标配浅蓝背景，修正标题间距、底部按钮样式和 CalDAV 标签宽度，并移除左侧“显示选项”按钮。

## 1.1.0 - 2026-06-26

- 日程管理入口改为使用待办管理同款图标，磁贴不再单独显示新增按钮。
- 日程管理窗口改为接近设计稿的布局：顶部搜索、左侧月历和筛选、右侧日程卡片列表、底部操作栏。
- 支持本地日历和 CalDAV 日历的新建、编辑；未配置 CalDAV 凭据时只创建本地日程。
- 删除入口收进编辑窗口，周期 CalDAV 日程删除时区分整组删除和仅本机隐藏本次。

## 1.0.3 - 2026-06-26

- 管理界面移除冗余的“筛选”按钮，搜索框继续在输入时实时刷新，并向右调整以填补按钮移除后的空位。
- README 当前发布版本、打包命令和发布包链接更新到 `1.0.3`。
- 日程磁贴的设置入口图标改为齿轮。

## 1.0.2 - 2026-06-26

- 论文设置窗口改为两页布局：`论文同步` 页用于配置同步地址、账号、密码、测试登录和保存同步；`标题翻译` 页用于配置 Tencent Cloud SecretId、SecretKey、测试连接和保存凭据。
- 论文设置窗口尺寸收回到 `620x560`，交互方式与日程设置的分页按钮保持一致，避免同步凭据和翻译凭据挤在同一个长表单中。
- 论文同步凭据继续保存到 `paper-sync.secret`，标题翻译凭据继续保存到 `translation.secret`，不改变现有 DPAPI 加密和运行时读取逻辑。
- 设置窗口新增 `关于` 页，显示当前版本号和 Full/Lite 版本类型，并提供 `检查更新`。
- 检查更新会读取 GitHub tags 判断最新版本，并从 GitHub Release 资产下载对应 ZIP；新版升级器同时下载 `.sha256` 并在解压前强制校验。
- 检查更新检测到新版本时会先询问是否更新；确认后才下载新版本包、自动解压并运行包内 `Install-Skins.ps1` 部署到当前 Rainmeter 安装目录。安装脚本部署后会自动重启 Rainmeter 并重新激活 Todo/Calendar 皮肤。
- Todo 磁贴设置按钮的提示文案从“翻译设置”改为“待办设置”，和当前设置窗口包含的内容保持一致。

## 1.0.1 - 2026-06-26

- 修复日程设置的“自动转入”规则列表在高 DPI 或字体度量变化时文字被截断的问题。规则列表现在按实际字体计算行高并垂直居中绘制，避免选中条和文字高度不一致。
- 改进待办编辑器的标签选择：新增/修改待办时可直接维护 `论文`、`日程`、`已读`、`自动归档` 等通用标签；标签区默认保持单行紧凑，展开时浮在备注区域上层，收起后继续编辑备注。
- 完善论文待办的阅读状态语义：手动完成论文标记为 `已读`，06:00 日切自动完成的旧论文标记为 `自动归档`，恢复论文时会移除这两个状态标签并刷新创建时间。
- 优化 06:00 日切和时间边界刷新：保留桌面交互流畅度，同时减少后台宿主进程频繁启动；电脑休眠、错过整点或重新加载皮肤后会在下一次检查补做日切。
- 完善今日日程到待办的一次性/系列转换：CalDAV 提醒映射为待办开始时间，日程结束映射为截止时间，转换备注会保留原始时间、地点和说明，会议链接优先作为可点击目标。
- 修复 CalDAV UTC 时间解析导致的 8 小时时差问题，并在更新遇到 stale ETag 时重新读取当前资源后再重试。
- 提升桌面磁贴渲染清晰度：部署后保持不透明窗口并启用 Rainmeter DPI 感知，减少高 DPI 桌面上的模糊显示。

## 1.0.0

- 初始发布完整功能版和精简版安装包。
